<p id="notice"><%= notice %></p>

<p>
  <strong>Title:</strong>
  <%= @package.title %>
</p>
<br>
<p>
  <strong>Description:</strong>
  <%= @package.description %>
</p>
<br>
<p>
  <strong>Price:</strong>
  $<%= @package.amount %>
</p>

<%= link_to "Pay Now", paypal_url(@package) %> <br><br>

<%= link_to 'Edit', edit_package_path(@package) %> |
<%= link_to 'Back', packages_path %>
